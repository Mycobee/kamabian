---
- name: Update apt packages
  ansible.builtin.apt:
    update_cache: true

- name: Install defined apt packages
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop: "{{ apt_packages | default([]) }}"
